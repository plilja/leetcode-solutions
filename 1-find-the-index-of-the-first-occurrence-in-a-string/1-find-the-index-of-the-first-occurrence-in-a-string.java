class Solution {
    public int strStr(String haystack, String needle) {
        // probably not what they are looking for but instructions doesn't state
        // that it doesn't want this ...
        return haystack.indexOf(needle);
    }
}
